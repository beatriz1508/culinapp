<h1>Escolha a trilha que deseja seguir</h1>
<ul>
  <% @paths.each do |path| %>
  <li><%= path.title %></li>
    <%= simple_form_for [path, @journey] do |f| %>
      <%= f.submit "entrar na trilha" %>
    <% end %>
  <% end %>
</ul>

